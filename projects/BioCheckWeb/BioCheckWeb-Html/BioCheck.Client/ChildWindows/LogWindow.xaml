<controls:ChildWindow
  x:Class="BioCheck.LogWindow"
  xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
  xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
  xmlns:controls="clr-namespace:System.Windows.Controls;assembly=System.Windows.Controls"  
  Title="Analysis Log"
  Width="600"
  Height="600"
  Style="{StaticResource ErrorWindowStyle}">

    <Grid x:Name="LayoutRoot" >
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBox x:Name="LogTextBox" Grid.Row="0" Margin="0,6,0,0" Height="Auto" Width="Auto" HorizontalAlignment="Stretch"
                 VerticalAlignment="Stretch"
                 TextWrapping="Wrap" IsReadOnly="True"
                 VerticalScrollBarVisibility="Auto" />

        <Button x:Name="OKButton" Grid.Row="1" Click="OKButton_Click" 
            Width="75" Height="23" HorizontalAlignment="Right" Margin="0,10,0,0" 
            TabIndex="0" Content="{Binding Path=Strings.OKButton, Source={StaticResource ApplicationResources}}" />

    </Grid>

</controls:ChildWindow>