{
  "version": "0.1.0",
  "main": "lib/app.js",
  "private": true,
  "scripts": {
    "compile": "tsc",
    "postinstall": "typings install && npm run compile",
    "watch": "tsc --watch",
    "start": "node lib/app.js"
  },
  "dependencies": {
    "botbuilder": "^3.1.1",
    "restify": "^4.1.1"
  },
  "devDependencies": {
    "typescript": "^1.8.10",
    "typings": "^1.3.2"
  }
}
