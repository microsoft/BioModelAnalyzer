{
  "name": "bmachatbot",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "tsc",
    "postinstall": "typings install && npm run compile",
    "watch": "tsc --watch",
    "start": "node lib/app.js"
  },
  "dependencies": {
    "botbuilder": "^3.1.1",
    "config": "^1.21.0",
    "restify": "^4.1.1"
  },
  "devDependencies": {
    "typescript": "^1.8.10",
    "typings": "^1.3.2"
  }
}
