{
  "name": "bmachatbot",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "compile": "tsc",
    "postinstall": "typings install && npm run compile",
    "test": "mocha --require ./test/init.js test/**/*.ts",
    "watch": "tsc --watch",
    "start": "node lib/app.js"
  },
  "dependencies": {
    "botbuilder": "^3.1.1",
    "config": "^1.21.0",
    "js-yaml": "^3.6.1",
    "restify": "^4.1.1"
  },
  "devDependencies": {
    "mocha": "^3.0.1",
    "ts-node": "^1.2.2",
    "typescript": "^1.8.10",
    "typings": "^1.3.2"
  }
}
